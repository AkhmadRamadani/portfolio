version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: portfolio-website
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GOOGLE_SHEET_URL=
    restart: unless-stopped